q1  <- quantile(x, 0.25, na.rm = TRUE)
q2  <- median(x, na.rm = TRUE)
q3  <- quantile(x, 0.75, na.rm = TRUE)
iqr <- IQR(x, na.rm = TRUE)
# place text ABOVE all data points
y_pos <- max(x, na.rm = TRUE) + 8
text(
i, y_pos,
labels = paste0(
"Q1=", round(q1,1), "\n",
"Med=", round(q2,1), "\n",
"Q3=", round(q3,1), "\n",
"IQR=", round(iqr,1)
),
cex = 0.8
)
}
boxplot(
Weight ~ PhysActive,
data = nhanes.samp.adult.500,
main = "Weight vs Physical Activity",
xlab = "Physically Active",
ylab = "Weight (kg)",
col = c("blue", "grey"),
ylim = c(40, 300)   # ⬅ increase top space
)
groups <- levels(nhanes.samp.adult.500$PhysActive)
for (i in seq_along(groups)) {
x <- nhanes.samp.adult.500$Weight[
nhanes.samp.adult.500$PhysActive == groups[i]
]
q1  <- quantile(x, 0.25, na.rm = TRUE)
q2  <- median(x, na.rm = TRUE)
q3  <- quantile(x, 0.75, na.rm = TRUE)
iqr <- IQR(x, na.rm = TRUE)
# place text ABOVE all data points
y_pos <- max(x, na.rm = TRUE) +4
text(
i, y_pos,
labels = paste0(
"Q1=", round(q1,1), "\n",
"Med=", round(q2,1), "\n",
"Q3=", round(q3,1), "\n",
"IQR=", round(iqr,1)
),
cex = 0.8
)
}
boxplot(
Weight ~ PhysActive,
data = nhanes.samp.adult.500,
main = "Weight vs Physical Activity",
xlab = "Physically Active",
ylab = "Weight (kg)",
col = c("blue", "grey"),
ylim = c(40, 300)   # ⬅ increase top space
)
groups <- levels(nhanes.samp.adult.500$PhysActive)
for (i in seq_along(groups)) {
x <- nhanes.samp.adult.500$Weight[
nhanes.samp.adult.500$PhysActive == groups[i]
]
q1  <- quantile(x, 0.25, na.rm = TRUE)
q2  <- median(x, na.rm = TRUE)
q3  <- quantile(x, 0.75, na.rm = TRUE)
iqr <- IQR(x, na.rm = TRUE)
# place text ABOVE all data points
y_pos <- max(x, na.rm = TRUE) +4
text(
i, y_pos,
labels = paste0(
"Q1=", round(q1,1), "\n",
"Med=", round(q2,1), "\n",
"Q3=", round(q3,1), "\n",
"IQR=", round(iqr,1)
),
cex = 0.5
)
}
boxplot(
Weight ~ PhysActive,
data = nhanes.samp.adult.500,
main = "Weight vs Physical Activity",
xlab = "Physically Active",
ylab = "Weight (kg)",
col = c("blue", "grey"),
ylim = c(40, 300)   # ⬅ increase top space
)
groups <- levels(nhanes.samp.adult.500$PhysActive)
for (i in seq_along(groups)) {
x <- nhanes.samp.adult.500$Weight[
nhanes.samp.adult.500$PhysActive == groups[i]
]
q1  <- quantile(x, 0.25, na.rm = TRUE)
q2  <- median(x, na.rm = TRUE)
q3  <- quantile(x, 0.75, na.rm = TRUE)
iqr <- IQR(x, na.rm = TRUE)
# place text ABOVE all data points
y_pos <- max(x, na.rm = TRUE) +4
text(
i, y_pos,
labels = paste0(
"Q1=", round(q1,1), "\n",
"Med=", round(q2,1), "\n",
"Q3=", round(q3,1), "\n",
"IQR=", round(iqr,1)
),
cex = 1
)
}
boxplot(
Weight ~ PhysActive,
data = nhanes.samp.adult.500,
main = "Weight vs Physical Activity",
xlab = "Physically Active",
ylab = "Weight (kg)",
col = c("blue", "grey"),
ylim = c(40, 300)   # ⬅ increase top space
)
groups <- levels(nhanes.samp.adult.500$PhysActive)
for (i in seq_along(groups)) {
x <- nhanes.samp.adult.500$Weight[
nhanes.samp.adult.500$PhysActive == groups[i]
]
q1  <- quantile(x, 0.25, na.rm = TRUE)
q2  <- median(x, na.rm = TRUE)
q3  <- quantile(x, 0.75, na.rm = TRUE)
iqr <- IQR(x, na.rm = TRUE)
# place text ABOVE all data points
y_pos <- max(x, na.rm = TRUE)
text(
i, y_pos,
labels = paste0(
"Q1=", round(q1,1), "\n",
"Med=", round(q2,1), "\n",
"Q3=", round(q3,1), "\n",
"IQR=", round(iqr,1)
),
cex = 1
)
}
boxplot(
Weight ~ PhysActive,
data = nhanes.samp.adult.500,
main = "Weight vs Physical Activity",
xlab = "Physically Active",
ylab = "Weight (kg)",
col = c("blue", "grey"),
ylim = c(40, 300)   # ⬅ increase top space
)
groups <- levels(nhanes.samp.adult.500$PhysActive)
for (i in seq_along(groups)) {
x <- nhanes.samp.adult.500$Weight[
nhanes.samp.adult.500$PhysActive == groups[i]
]
q1  <- quantile(x, 0.25, na.rm = TRUE)
q2  <- median(x, na.rm = TRUE)
q3  <- quantile(x, 0.75, na.rm = TRUE)
iqr <- IQR(x, na.rm = TRUE)
# place text ABOVE all data points
y_pos <- max(x, na.rm = TRUE) +15
text(
i, y_pos,
labels = paste0(
"Q1=", round(q1,1), "\n",
"Med=", round(q2,1), "\n",
"Q3=", round(q3,1), "\n",
"IQR=", round(iqr,1)
),
cex = 1
)
}
text(
i - 0.15, y_pos,
labels = paste0(
"Q1=", round(q1,1), "\n",
"Med=", round(q2,1), "\n",
"Q3=", round(q3,1), "\n",
"IQR=", round(iqr,1)
),
cex = 1
)
boxplot(
Weight ~ PhysActive,
data = nhanes.samp.adult.500,
main = "Weight vs Physical Activity",
xlab = "Physically Active",
ylab = "Weight (kg)",
col = c("blue", "grey"),
ylim = c(40, 300)   # ⬅ increase top space
)
groups <- levels(nhanes.samp.adult.500$PhysActive)
for (i in seq_along(groups)) {
x <- nhanes.samp.adult.500$Weight[
nhanes.samp.adult.500$PhysActive == groups[i]
]
q1  <- quantile(x, 0.25, na.rm = TRUE)
q2  <- median(x, na.rm = TRUE)
q3  <- quantile(x, 0.75, na.rm = TRUE)
iqr <- IQR(x, na.rm = TRUE)
# place text ABOVE all data points
y_pos <- max(x, na.rm = TRUE) +15
text(
i - 0.15, y_pos,
labels = paste0(
"Q1=", round(q1,1), "\n",
"Med=", round(q2,1), "\n",
"Q3=", round(q3,1), "\n",
"IQR=", round(iqr,1)
),
cex = 1
)
}
boxplot(
Weight ~ PhysActive,
data = nhanes.samp.adult.500,
main = "Weight vs Physical Activity",
xlab = "Physically Active",
ylab = "Weight (kg)",
col = c("blue", "grey"),
ylim = c(40, 300)   # ⬅ increase top space
)
groups <- levels(nhanes.samp.adult.500$PhysActive)
for (i in seq_along(groups)) {
x <- nhanes.samp.adult.500$Weight[
nhanes.samp.adult.500$PhysActive == groups[i]
]
q1  <- quantile(x, 0.25, na.rm = TRUE)
q2  <- median(x, na.rm = TRUE)
q3  <- quantile(x, 0.75, na.rm = TRUE)
iqr <- IQR(x, na.rm = TRUE)
# place text ABOVE all data points
y_pos <- max(x, na.rm = TRUE) +20
text(
i - 0.15, y_pos,
labels = paste0(
"Q1=", round(q1,1), "\n",
"Med=", round(q2,1), "\n",
"Q3=", round(q3,1), "\n",
"IQR=", round(iqr,1)
),
cex = 1
)
}
boxplot(
Weight ~ PhysActive,
data = nhanes.samp.adult.500,
main = "Weight vs Physical Activity",
xlab = "Physically Active",
ylab = "Weight (kg)",
col = c("blue", "grey"),
ylim = c(40, 350)   # ⬅ increase top space
)
---
title: "Activity 4_2"
boxplot(
Weight ~ PhysActive,
data = nhanes.samp.adult.500,
main = "Weight vs Physical Activity",
xlab = "Physically Active",
ylab = "Weight (kg)",
col = c("blue", "grey"),
ylim = c(40, 350)   # ⬅ increase top space
)
groups <- levels(nhanes.samp.adult.500$PhysActive)
for (i in seq_along(groups)) {
x <- nhanes.samp.adult.500$Weight[
nhanes.samp.adult.500$PhysActive == groups[i]
]
q1  <- quantile(x, 0.25, na.rm = TRUE)
q2  <- median(x, na.rm = TRUE)
q3  <- quantile(x, 0.75, na.rm = TRUE)
iqr <- IQR(x, na.rm = TRUE)
# place text ABOVE all data points
y_pos <- max(x, na.rm = TRUE) +20
text(
i - 0.15, y_pos,
labels = paste0(
"Q1=", round(q1,1), "\n",
"Med=", round(q2,1), "\n",
"Q3=", round(q3,1), "\n",
"IQR=", round(iqr,1)
),
cex = 1
)
}
boxplot(
Weight ~ PhysActive,
data = nhanes.samp.adult.500,
main = "Weight vs Physical Activity",
xlab = "Physically Active",
ylab = "Weight (kg)",
col = c("blue", "grey"),
ylim = c(4),   # ⬅ increase top space,
)
groups <- levels(nhanes.samp.adult.500$PhysActive)
for (i in seq_along(groups)) {
x <- nhanes.samp.adult.500$Weight[
nhanes.samp.adult.500$PhysActive == groups[i]
]
q1  <- quantile(x, 0.25, na.rm = TRUE)
q2  <- median(x, na.rm = TRUE)
q3  <- quantile(x, 0.75, na.rm = TRUE)
iqr <- IQR(x, na.rm = TRUE)
# place text ABOVE all data points
y_pos <- max(x, na.rm = TRUE) +20
text(
i - 0.15, y_pos,
labels = paste0(
"Q1=", round(q1,1), "\n",
"Med=", round(q2,1), "\n",
"Q3=", round(q3,1), "\n",
"IQR=", round(iqr,1)
),
cex = 1
)
}
boxplot(
Weight ~ PhysActive,
data = nhanes.samp.adult.500,
main = "Weight vs Physical Activity",
xlab = "Physically Active",
ylab = "Weight (kg)",
col = c("blue", "grey"),
ylim = c(4)   # ⬅ increase top space,
)
groups <- levels(nhanes.samp.adult.500$PhysActive)
for (i in seq_along(groups)) {
x <- nhanes.samp.adult.500$Weight[
nhanes.samp.adult.500$PhysActive == groups[i]
]
q1  <- quantile(x, 0.25, na.rm = TRUE)
q2  <- median(x, na.rm = TRUE)
q3  <- quantile(x, 0.75, na.rm = TRUE)
iqr <- IQR(x, na.rm = TRUE)
# place text ABOVE all data points
y_pos <- max(x, na.rm = TRUE) +20
text(
i - 0.15, y_pos,
labels = paste0(
"Q1=", round(q1,1), "\n",
"Med=", round(q2,1), "\n",
"Q3=", round(q3,1), "\n",
"IQR=", round(iqr,1)
),
cex = 1
)
}
boxplot(
Weight ~ PhysActive,
data = nhanes.samp.adult.500,
main = "Weight vs Physical Activity",
xlab = "Physically Active",
ylab = "Weight (kg)",
col = c("blue", "grey"),
ylim = c(4)   # ⬅ increase top space,
)
groups <- levels(nhanes.samp.adult.500$PhysActive)
for (i in seq_along(groups)) {
x <- nhanes.samp.adult.500$Weight[
nhanes.samp.adult.500$PhysActive == groups[i]
]
q1  <- quantile(x, 0.25, na.rm = TRUE)
q2  <- median(x, na.rm = TRUE)
q3  <- quantile(x, 0.75, na.rm = TRUE)
iqr <- IQR(x, na.rm = TRUE)
# place text ABOVE all data points
y_pos <- max(x, na.rm = TRUE) +20
text(
i - 0.15, y_pos,
labels = paste0(
"Q1=", round(q1,1), "\n",
"Med=", round(q2,1), "\n",
"Q3=", round(q3,1), "\n",
"IQR=", round(iqr,1)
),
cex = 1
)
}
boxplot(
Weight ~ PhysActive,
data = nhanes.samp.adult.500,
main = "Weight vs Physical Activity",
xlab = "Physically Active",
ylab = "Weight (kg)",
col = c("blue", "grey"),
ylim = c(0, 350)   # ⬅ increase top space
)
groups <- levels(nhanes.samp.adult.500$PhysActive)
for (i in seq_along(groups)) {
x <- nhanes.samp.adult.500$Weight[
nhanes.samp.adult.500$PhysActive == groups[i]
]
q1  <- quantile(x, 0.25, na.rm = TRUE)
q2  <- median(x, na.rm = TRUE)
q3  <- quantile(x, 0.75, na.rm = TRUE)
iqr <- IQR(x, na.rm = TRUE)
# place text ABOVE all data points
y_pos <- max(x, na.rm = TRUE) +20
text(
i - 0.15, y_pos,
labels = paste0(
"Q1=", round(q1,1), "\n",
"Med=", round(q2,1), "\n",
"Q3=", round(q3,1), "\n",
"IQR=", round(iqr,1)
),
cex = 1
)
}
boxplot(
Weight ~ PhysActive,
data = nhanes.samp.adult.500,
main = "Weight vs Physical Activity",
xlab = "Physically Active",
ylab = "Weight (kg)",
col = c("blue", "grey"),
ylim = c(0, 350),
yaxt = "n"   # turn off default y-axis
)
# Add y-axis with ticks every 25
axis(2, at = seq(0, 350, by = 25))
groups <- levels(nhanes.samp.adult.500$PhysActive)
for (i in seq_along(groups)) {
x <- nhanes.samp.adult.500$Weight[
nhanes.samp.adult.500$PhysActive == groups[i]
]
q1  <- quantile(x, 0.25, na.rm = TRUE)
q2  <- median(x, na.rm = TRUE)
q3  <- quantile(x, 0.75, na.rm = TRUE)
iqr <- IQR(x, na.rm = TRUE)
# place text ABOVE all data points
y_pos <- max(x, na.rm = TRUE) +20
text(
i - 0.15, y_pos,
labels = paste0(
"Q1=", round(q1,1), "\n",
"Med=", round(q2,1), "\n",
"Q3=", round(q3,1), "\n",
"IQR=", round(iqr,1)
),
cex = 1
)
}
boxplot(
Weight ~ PhysActive,
data = nhanes.samp.adult.500,
main = "Weight vs Physical Activity",
xlab = "Physically Active",
ylab = "Weight (kg)",
col = c("blue", "grey"),
ylim = c(0, 350),
yaxt = "n"   # turn off default y-axis
)
# Add y-axis with ticks every 25
axis(2, at = seq(0, 350, by = 10))
groups <- levels(nhanes.samp.adult.500$PhysActive)
for (i in seq_along(groups)) {
x <- nhanes.samp.adult.500$Weight[
nhanes.samp.adult.500$PhysActive == groups[i]
]
q1  <- quantile(x, 0.25, na.rm = TRUE)
q2  <- median(x, na.rm = TRUE)
q3  <- quantile(x, 0.75, na.rm = TRUE)
iqr <- IQR(x, na.rm = TRUE)
# place text ABOVE all data points
y_pos <- max(x, na.rm = TRUE) +20
text(
i - 0.15, y_pos,
labels = paste0(
"Q1=", round(q1,1), "\n",
"Med=", round(q2,1), "\n",
"Q3=", round(q3,1), "\n",
"IQR=", round(iqr,1)
),
cex = 1
)
}
boxplot(
Weight ~ PhysActive,
data = nhanes.samp.adult.500,
main = "Weight vs Physical Activity",
xlab = "Physically Active",
ylab = "Weight (kg)",
col = c("blue", "grey"),
ylim = c(0, 300),
yaxt = "n"   # turn off default y-axis
)
# Add y-axis with ticks every 25
axis(2, at = seq(0, 350, by = 10))
groups <- levels(nhanes.samp.adult.500$PhysActive)
for (i in seq_along(groups)) {
x <- nhanes.samp.adult.500$Weight[
nhanes.samp.adult.500$PhysActive == groups[i]
]
q1  <- quantile(x, 0.25, na.rm = TRUE)
q2  <- median(x, na.rm = TRUE)
q3  <- quantile(x, 0.75, na.rm = TRUE)
iqr <- IQR(x, na.rm = TRUE)
# place text ABOVE all data points
y_pos <- max(x, na.rm = TRUE) +20
text(
i - 0.15, y_pos,
labels = paste0(
"Q1=", round(q1,1), "\n",
"Med=", round(q2,1), "\n",
"Q3=", round(q3,1), "\n",
"IQR=", round(iqr,1)
),
cex = 1
)
}
